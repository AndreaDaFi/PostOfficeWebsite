import React from 'react';
import { Typography } from '@mui/material';

const AddStore = () => {
  return (
    <div style={{ padding: 20 }}>
      <Typography variant="h4" gutterBottom>
        Page so customers can view a specific packages details: so cost, transaction stuff, if they bought any items, origin and destination address, all that
      </Typography>
    </div>
  );
};

export default AddStore;