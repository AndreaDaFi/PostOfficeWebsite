import React from 'react';
import { Typography } from '@mui/material';

const EmpLogin = () => {
  return (
    <div style={{ padding: 20 }}>
      <Typography variant="h4" gutterBottom>
        Page so Admin can view all Post Offices
      </Typography>
    </div>
  );
};

export default EmpLogin;