import React from 'react';
import { Typography } from '@mui/material';

const AddMngr = () => {
  return (
    <div style={{ padding: 20 }}>
      <Typography variant="h4" gutterBottom>
        Page so Drivers can update the status of a package
      </Typography>
    </div>
  );
};

export default AddMngr;